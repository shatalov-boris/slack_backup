.container
  .row
    .col-md-12
      - flash.each do |name, msg|
        - if msg.is_a?(String)
          %div.center-alert{class: "alert alert-#{name.to_s == "notice" ? "info" : "danger"}"}
            %a.close{"data-dismiss": "alert"} ×
            = content_tag :div, msg.html_safe, id: "flash_#{name}"
